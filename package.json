{
  "name": "blast-ide-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "flash",
    "blast"
  ],
  "scripts": {
    "dev:blast": "npm run dev --workspace=blast -- --host 0.0.0.0 --port 5188",
    "build:flash": "npm run build --workspace=flash"
  }
}
